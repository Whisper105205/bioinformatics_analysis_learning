# 导入数据
```R

```
[[数据输入]]
# 清洗数据（删除缺失值、重复的、异常的；重编码；统一格式）
```R
# 缺失值
is.na()
na.omit()#删除含缺失值的一整行
# 统计函数通用参数na.rm=TRUE（跳过缺失值）
sum( ,na.rm=TRUE) 
prod( ,na.rm=TRUE)#连乘
```

```R
# 重编码（改分类标签，数值分组，）
 # 直接用赋值语句覆盖/新建
e.g. leadership<-within(leadership,
agecat<-NA
agecat[age>75]<-"Elder"
agecat[age<=75 & age>=55]<-"Middle Aged"
agecat[age<55]<-"Young")

# 用dplyr包等内函数
leadership$agecat<-recode(leadership$age,)
```
[[基本数据管理]]

# 数据转换（）

# 分析数据
## 依需要求均值、标准差等
